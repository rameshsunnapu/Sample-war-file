@Library('rameshsunnapuSharedLibrary') _
pipeline {
    agent any

    stages {
        stage('Start') {
            steps {
                echo "Pipeline started!"
                PrintMessage "Hello from Jenkins! Shared Library"
            }
        }
    }
}
